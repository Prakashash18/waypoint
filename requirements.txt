# Pinned to the versions this app is actually tested against
# (tests/api_smoke.py, verified 2026-08-30). The previous pins were well
# behind — openai>=1.50.0 would have installed 1.x, a different API from the
# 3.x the agent's tool-calling loop is written for.

flask==3.1.3
flask-cors==6.0.5
python-dotenv==1.2.3
requests==2.34.2
openai==3.5.0
pydantic==2.13.4
email-validator==2.3.0
gunicorn==26.2.0

# Live screenshots of hotel websites and rendered map views.
# The Chromium binary is installed separately: playwright install chromium
playwright==1.62.0
